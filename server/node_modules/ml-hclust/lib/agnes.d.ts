import { Cluster } from './cluster.ts';
export type AgglomerationMethod = 'single' | 'complete' | 'average' | 'upgma' | 'wpgma' | 'median' | 'wpgmc' | 'centroid' | 'upgmc' | 'ward' | 'ward2';
type DistanceFunction<T> = (a: T, b: T) => number;
export interface AgnesOptions<T> {
    distanceFunction?: DistanceFunction<T>;
    /**
     * @default 'complete'
     */
    method?: AgglomerationMethod;
    /**
     * Is the input already a distance matrix?
     */
    isDistanceMatrix?: boolean;
}
/**
 * Continuously merge nodes that have the least dissimilarity
 * @param data - Array of points to be clustered
 * @param options
 */
export declare function agnes<T>(data: T[], options?: AgnesOptions<T>): Cluster;
export {};
//# sourceMappingURL=agnes.d.ts.map